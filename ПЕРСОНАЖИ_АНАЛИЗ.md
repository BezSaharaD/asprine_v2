# Полный анализ структуры персонажей для калькулятора Genshin Impact

## Обзор архитектуры

Персонаж в калькуляторе состоит из нескольких ключевых компонентов:

1. **Файл персонажа** (`src/js/db/Char/[Name].js`) - основная логика и данные
2. **Таблицы статов** (`src/js/db/generated/CharTables.js`) - базовые характеристики по уровням
3. **Таблицы талантов** (`src/js/db/generated/CharTalentTables.js`) - множители талантов по уровням
4. **Локализация** (`data/strings/[version]/[name].csv` или `data/strings/char/[name].csv`) - названия и описания

---

## Структура файла персонажа (DbObjectChar)

### Базовые поля

```javascript
new DbObjectChar({
    name: 'имя_перса',           // Уникальный идентификатор (snake_case)
    serializeId: 109,            // Уникальный числовой ID для сериализации
    gameId: 10000121,            // ID из игры (для импорта)
    iconClass: 'char-icon-имя',  // CSS класс иконки
    rarity: 4,                   // Редкость: 4 или 5
    element: 'hydro',            // Элемент: pyro, hydro, cryo, electro, anemo, geo, dendro
    weapon: 'claymore',          // Оружие: sword, claymore, polearm, bow, catalyst
    origin: 'nodkrai',           // Регион: mondstadt, liyue, inazuma, sumeru, fontaine, natlan, snezhnaya, nodkrai
    talents: Talents,            // Объект DbObjectTalents
    statTable: charTables.Name,  // Таблица базовых статов
    features: [],                // Массив Feature (урон, хил, щиты)
    conditions: [],              // Массив Condition (баффы, пассивки)
    postEffects: [],             // Массив PostEffect (динамические бонусы)
    multipliers: [],             // Глобальные множители урона
    constellation: new DbObjectConstellation([...]), // Созвездия
    partyData: {...},            // Данные для баффов на отряд (опционально)
})
```

---

## Таланты (DbObjectTalents)

Таланты определяют множители урона и информацию для отображения:

```javascript
const Talents = new DbObjectTalents({
    attack: {
        gameId: charTalentTables.Name.s1_id,  // ID таланта из игры
        title: 'talent_name.name_attack',      // Ключ локализации названия
        description: 'talent_descr.name_attack', // Ключ локализации описания
        items: [
            {
                table: new StatTable('normal_hit_1', charTalentTables.Name.s1.p1),
            },
            {
                table: new StatTable('normal_hit_2', charTalentTables.Name.s1.p2),
            },
            // ... остальные удары
            {
                table: new StatTable('charged_hit', charTalentTables.Name.s1.p5),
            },
            {
                unit: 'unit',  // Единица измерения для отображения
                table: new StatTable('stamina_cost', charTalentTables.Name.s1.p6),
            },
            {
                table: new StatTable('plunge', charTalentTables.Name.s1.p7),
            },
            {
                table: new StatTable('plunge_low', charTalentTables.Name.s1.p8),
            },
            {
                table: new StatTable('plunge_high', charTalentTables.Name.s1.p9),
            },
        ],
    },
    skill: {
        gameId: charTalentTables.Name.s2_id,
        title: 'talent_name.name_skill',
        description: 'talent_descr.name_skill',
        items: [
            {
                table: new StatTable('skill_dmg', charTalentTables.Name.s2.p1),
            },
            {
                unit: 'sec',
                table: new StatTable('cd', charTalentTables.Name.s2.p2),
            },
        ],
    },
    burst: {
        gameId: charTalentTables.Name.s3_id,
        title: 'talent_name.name_burst',
        description: 'talent_descr.name_burst',
        items: [
            {
                table: new StatTable('burst_dmg', charTalentTables.Name.s3.p1),
            },
            {
                unit: 'sec',
                table: new StatTable('duration', charTalentTables.Name.s3.p2),
            },
            {
                unit: 'sec',
                table: new StatTable('cd', charTalentTables.Name.s3.p3),
            },
            {
                unit: '',  // Пустая единица для энергии
                table: new StatTable('energy_cost', charTalentTables.Name.s3.p4),
            },
        ],
    },
});
```

### Специальные типы items

```javascript
// Множественные значения (например, ATK% + EM)
{
    type: 'multivalue',
    separator: ' + ',
    units: ['atk', 'mastery'],
    table: [
        new StatTable('dmg_atk', values1),
        new StatTable('dmg_mastery', values2),
    ],
}

// Щит (HP% + плоское значение)
{
    type: 'shield',
    unit: 'hp',
    table: [
        new StatTable('shield_percent', values1),
        new StatTable('shield_flat', values2),
    ],
}

// Множественные хиты
{
    type: 'hits',
    table: [
        new StatTable('hit_1', values1),
        new StatTable('hit_2', values2),
    ],
}
```

### Доступные единицы измерения (unit)

| unit | Русский | English |
|------|---------|---------|
| (не указан) | % | % |
| `''` (пустая строка) | (без единицы) | (no unit) |
| `sec` | сек | s |
| `per_sec` | ед/сек | /s |
| `unit` | ед | (space) |
| `hp` | % от макс. HP | % Max HP |
| `atk` | % атаки | % ATK |
| `def` | % защиты | % DEF |
| `mastery` | % МС | % EM |
| `current_hp` | % текущего HP | % Current HP |
| `per_stack` | /уровень | /Stack |
| `percent_per_stack` | %/ур. | % per stack |
| `per_energy` | % за энергию | % Per Energy |
| `normal_atk` | % от урона обычной атаки | % Normal Attack DMG |
| `charged_attack` | % от урона заряж. атаки | % Charged Attack DMG |
| `bond` | % Долга жизни | % Bond of Life |
| `atk_nightsoul` | % силы атаки за оч. Ночного духа | % ATK / Nightsoul Point |

---

## Features (Расчёт урона/хила/щитов)

### Типы Feature для урона

```javascript
// Обычные атаки (с инфузией)
new FeatureDamageNormal({
    name: 'normal_hit_1',
    // element: 'phys' по умолчанию, но может быть инфузирован
    multipliers: [
        new FeatureMultiplier({
            leveling: 'char_skill_attack',
            values: Talents.get('attack.normal_hit_1'),
        }),
    ],
})

// Заряженная атака
new FeatureDamageCharged({
    name: 'charged_hit',
    multipliers: [...],
})

// Атака в падении
new FeatureDamagePlungeCollision({name: 'plunge', ...})
new FeatureDamagePlungeShockWave({name: 'plunge_low', ...})
new FeatureDamagePlungeShockWave({name: 'plunge_high', ...})

// Элементальный навык
new FeatureDamageSkill({
    name: 'skill_dmg',
    element: 'pyro',  // Обязательно указать элемент
    multipliers: [...],
})

// Взрыв стихий
new FeatureDamageBurst({
    name: 'burst_dmg',
    element: 'hydro',
    multipliers: [...],
})
```

### FeatureMultiplier - множители урона

```javascript
new FeatureMultiplier({
    // Скейлинг (от какого стата считается урон)
    scaling: 'atk*',      // ATK (по умолчанию)
    scaling: 'hp*',       // HP
    scaling: 'def*',      // DEF
    scaling: 'mastery*',  // Elemental Mastery
    
    // Прокачка (какой талант влияет на множитель)
    leveling: 'char_skill_attack',     // Обычная атака
    leveling: 'char_skill_elemental',  // Элементальный навык
    leveling: 'char_skill_burst',      // Взрыв стихий
    leveling: 'weapon_refine',         // Пробуждение оружия
    
    // Значения множителей по уровням
    values: Talents.get('attack.normal_hit_1'),
    // или
    values: new ValueTable([100, 110, 120, ...]),
    
    // Источник (для отображения в деталях)
    source: 'constellation6',
    source: 'ascension4',
    
    // Условие активации
    condition: new ConditionConstellation({constellation: 6}),
    
    // Стаки
    stacksLeveling: 'buff_stacks',
    maxStacks: 5,
})
```

### Дополнительные параметры Feature

```javascript
new FeatureDamageSkill({
    name: 'skill_dmg',
    element: 'hydro',
    
    // Дополнительные бонусы урона (суммируются с dmg_all, dmg_element, dmg_skill)
    damageBonuses: ['dmg_skill_custom_bonus'],
    
    // Дополнительные бонусы крит. шанса
    critRateBonuses: ['crit_rate_custom'],
    
    // Дополнительные бонусы крит. урона
    critDamageBonuses: ['crit_dmg_custom'],
    
    // Не может реагировать (для некоторых атак)
    cannotReact: true,
    
    // Теги для таргетинга множителей
    tags: ['furina_attack'],
    
    multipliers: [...],
})
```

### Хил и щиты

```javascript
// Лечение
new FeatureHeal({
    category: 'skill',
    name: 'heal_amount',
    partyHeal: true,  // Лечит всю команду
    noSelfHeal: true, // Не лечит себя
    multipliers: [
        new FeatureMultiplier({
            scaling: 'hp*',
            leveling: 'char_skill_elemental',
            values: Talents.get('skill.heal_percent'),
        }),
        new FeatureMultiplier({
            scaling: 'flat',  // Плоское значение
            leveling: 'char_skill_elemental',
            values: Talents.get('skill.heal_flat'),
        }),
    ],
})

// Щит
new FeatureShield({
    category: 'skill',
    name: 'shield_hp',
    multipliers: [...],
})
```

---

## Conditions (Баффы и пассивки)

### Типы Condition

```javascript
// Статичное описание (не интерактивное)
new ConditionStatic({
    title: 'talent_name.passive_name',
    description: 'talent_descr.passive_description',
    info: {ascension: 4},  // Показывает "A4" в UI
    stats: {
        text_percent: 20,  // Переменные для описания
    },
    // Условие отображения
    condition: new ConditionAscensionChar({ascension: 4}),
})

// Чекбокс (вкл/выкл)
new ConditionBoolean({
    name: 'buff_name',           // Уникальное имя для сохранения
    serializeId: 1,              // ID для сериализации
    title: 'talent_name.buff',
    description: 'talent_descr.buff',
    stats: {
        dmg_all: 15,             // Статы при активации
        atk_percent: 10,
    },
    settings: {
        attack_infusion: 'pyro', // Настройки при активации
    },
    // Вложенные условия
    subConditions: [
        new ConditionAscensionChar({ascension: 1}),
        new ConditionBoolean({name: 'other_buff'}),
    ],
})

// Стаки
new ConditionStacks({
    name: 'stacks_name',
    serializeId: 2,
    title: 'talent_name.stacks',
    description: 'talent_descr.stacks',
    maxStacks: 5,  // Или функция: (settings) => settings.char_constellation >= 2 ? 7 : 5
    stats: [
        new StatTable('atk_percent', [10]),  // 10% за стак
        new StatTable('crit_rate', [5]),
    ],
})

// Числовое значение
new ConditionNumber({
    name: 'fanfare_stacks',
    serializeId: 3,
    title: 'talent_name.fanfare',
    max: 300,
    allowMinZero: true,
})

// Выбор уровня (dropdown)
new ConditionLevelSelect({
    name: 'buff_level',
    serializeId: 4,
    title: 'talent_name.buff_level',
    maxStacks: 4,
    stats: [
        new StatTable('mastery', [100, 120, 140, 160]),
    ],
})
```

### Условия проверки

```javascript
// Проверка созвездия
new ConditionConstellation({constellation: 2})

// Проверка возвышения
new ConditionAscensionChar({ascension: 4})

// Логическое И
new ConditionAnd([
    new ConditionBoolean({name: 'buff1'}),
    new ConditionBoolean({name: 'buff2'}),
])

// Логическое ИЛИ
new ConditionOr([...])

// Логическое НЕ
new ConditionNot(new ConditionBoolean({name: 'buff'}))
```

---

## PostEffects (Динамические бонусы)

PostEffects применяются после расчёта базовых статов:

```javascript
// Бонус от стата
new PostEffectStats({
    from: 'setting_name',        // Откуда берём значение
    levelSetting: 'char_skill_burst',
    percent: new StatTable('dmg_all', [0.1]),  // 0.1% за единицу
    statCap: new ValueTable([100]),  // Максимум бонуса
    conditions: [...],
})

// Бонус от HP
new PostEffectStatsHP({
    percent: new StatTable('dmg_skill', [0.0007]),  // 0.07% за 100 HP
    statCap: new StatTable('', [28]),  // Максимум 28%
    conditions: [...],
})

// Бонус от Mastery
new PostEffectStatsMastery({
    percent: new StatTable('crit_rate', [0.03]),
    exceed: 200,  // Считается только EM свыше 200
    global: true,
    statCap: new StatTable('', [24]),
    conditions: [...],
})
```

---

## Созвездия (DbObjectConstellation)

```javascript
new DbObjectConstellation([
    // C1
    {
        conditions: [
            new ConditionStatic({
                title: 'talent_name.c1',
                description: 'talent_descr.c1',
            }),
        ],
    },
    // C2
    {
        conditions: [
            new ConditionBoolean({
                name: 'c2_buff',
                serializeId: 5,
                title: 'talent_name.c2',
                description: 'talent_descr.c2',
                stats: {
                    crit_rate: 20,
                },
            }),
        ],
    },
    // C3 - бонус к навыку
    {
        conditions: [
            new Condition({
                settings: {
                    char_skill_elemental_bonus: 3,
                },
            }),
        ],
    },
    // C4
    {...},
    // C5 - бонус к взрыву
    {
        conditions: [
            new Condition({
                settings: {
                    char_skill_burst_bonus: 3,
                },
            }),
        ],
    },
    // C6
    {...},
])
```

---

## PartyData (Баффы на отряд)

```javascript
partyData: {
    // Какие настройки загружать из билда персонажа
    loadStats: {
        settings: ['char_skill_burst'],
    },
    
    conditions: [
        // Уровень таланта для расчёта баффа
        new ConditionNumberTalent({
            name: 'char_skill_burst',
            title: 'talent_name.stats_level_burst',
            partySetting: 'char_skill_burst',
            serializeId: 1,
        }),
        
        // Бафф на отряд
        new ConditionBoolean({
            name: 'party.buff_name',
            serializeId: 2,
            rotation: 'party',  // Показывать в секции Party
            title: 'talent_name.party_buff',
            description: 'talent_descr.party_buff',
            stats: {
                atk_percent: 25,
            },
        }),
    ],
    
    postEffects: [
        new PostEffectStats({...}),
    ],
}
```

---

## Локализация (CSV файлы)

Формат: `category;name;rus;eng`

```csv
category;name;rus;eng
char_name;name;Имя;Name

feature_attack;normal_hit_1;Урон 1-го удара;1-Hit DMG
feature_skill;skill_dmg;Урон навыка;Skill DMG
feature_burst;burst_dmg;Урон взрыва;Burst DMG

talent_name;name_attack;Название атаки;Attack Name
talent_descr;name_attack;Описание атаки;Attack Description

talent_name;name_c1;Название C1;C1 Name
talent_descr;name_c1;Описание C1;C1 Description
```

### Переменные в описаниях

```
%{text_percent}     - Значение из stats.text_percent
%{text_value}       - Значение из stats.text_value
%{text_percent|20}  - Значение или 20 по умолчанию

name{Имя}           - Выделение имени
pyro{Пиро}          - Выделение элемента (pyro, hydro, cryo, electro, anemo, geo, dendro)
skill{skill:Навык}  - Ссылка на навык
```

---

## Таблицы статов персонажа (CharTables)

Генерируются автоматически, структура:

```javascript
charTables.Name = [
    new StatTableAscensionScale({
        stat: 'atk_base',
        base: 26.6266,  // Базовое значение на 1 уровне
        ascension: new StatTable('', [22.8, 39.0, 60.6, 76.8, 93.1, 109.3]),
        scale: charScales.s5atk,  // Кривая роста
    }),
    new StatTableAscensionScale({
        stat: 'hp_base',
        base: 1000.986,
        ascension: new StatTable('', [...]),
        scale: charScales.s5hp,
    }),
    new StatTableAscensionScale({
        stat: 'def_base',
        base: 61.0266,
        ascension: new StatTable('', [...]),
        scale: charScales.s5hp,
    }),
    // Бонусный стат возвышения
    new StatTableAscensionScale({
        stat: 'crit_dmg_base',  // или crit_rate_base, atk_percent, hp_percent, mastery_base, recharge_base, dmg_element_base, healing_base
        base: 50,
        ascension: new StatTable('', [0, 9.6, 19.2, 19.2, 28.8, 38.4]),
    }),
    // Стоимость энергии
    new StatTableAscensionScale({
        stat: 'burst_energy_cost',
        base: 80,
    }),
    // Стоимость выносливости
    new StatTableAscensionScale({
        stat: 'charged_stamina_cost',
        base: 20,
    }),
]
```

---

## Таблицы талантов (CharTalentTables)

```javascript
charTalentTables.Name = {
    s1_id: 10386,  // ID таланта атаки
    s1: {
        p1: [36.7, 39.7, 42.7, ...],  // 15 значений для уровней 1-15
        p2: [...],
        // ...
    },
    s2_id: 10387,  // ID навыка
    s2: {
        p1: [...],
        // ...
    },
    s3_id: 10388,  // ID взрыва
    s3: {
        p1: [...],
        // ...
    },
}
```

---

## Регистрация персонажа

После создания файла персонажа, добавить в `src/js/db/Char.js`:

```javascript
import { NewChar } from "./Char/NewChar"

export const Chars = new DbObjectListSerializeChars({
    // ... существующие персонажи
    NewChar: NewChar, // 115
});
```

---

---

## Вывод данных в UI

### 1. Окно талантов (CharTalentModal)

Окно талантов показывает описание и таблицу значений по уровням. Данные берутся из `DbObjectTalents`:

```javascript
// Структура для отображения в окне талантов
const Talents = new DbObjectTalents({
    attack: {
        gameId: 10386,                              // ID для импорта
        title: 'talent_name.char_attack',           // Заголовок вкладки
        description: 'talent_descr.char_attack',    // Описание под заголовком
        items: [                                    // Таблица значений
            {
                // Простое значение (показывается как "36.7%")
                table: new StatTable('normal_hit_1', [36.7, 39.7, 42.7, ...]),
            },
            {
                // С единицей измерения (показывается как "20 ед.")
                unit: 'unit',  // unit, sec, per_sec, hp, atk, mastery, '' (пусто)
                table: new StatTable('stamina_cost', [20]),
            },
            {
                // С точностью (показывается как "1.60 сек")
                unit: 'sec',
                digits: 2,
                table: new StatTable('interval', [1.6, 1.55, 1.5, ...]),
            },
            {
                // Множественные хиты (показывается как "36.7% + 39.7%")
                type: 'hits',
                table: [
                    new StatTable('hit_1', [...]),
                    new StatTable('hit_2', [...]),
                ],
            },
            {
                // Щит: HP% + плоское (показывается как "10% + 1000")
                type: 'shield',
                unit: 'hp',
                table: [
                    new StatTable('shield_percent', [10, 11, 12, ...]),
                    new StatTable('shield_flat', [1000, 1100, 1200, ...]),
                ],
            },
            {
                // Мультизначение (показывается как "100% ATK + 50 EM")
                type: 'multivalue',
                separator: ' + ',
                units: ['atk', 'mastery'],
                table: [
                    new StatTable('dmg_atk', [100, 110, ...]),
                    new StatTable('dmg_mastery', [50, 55, ...]),
                ],
            },
            {
                // Разделённые значения (показывается в несколько строк)
                type: 'separated',
                separator: ' / ',  // или без separator для переноса строки
                table: [
                    new StatTable('value_1', [...]),
                    new StatTable('value_2', [...]),
                ],
            },
            {
                // Множественные хиты с суммой
                type: 'multihit',
                hits: 3,  // Показывается как "36.7% * 3"
                table: new StatTable('hit_dmg', [...]),
            },
        ],
    },
    skill: {...},
    burst: {...},
    // Дополнительные вкладки (пассивки)
    links: [10389, 10390],  // ID пассивок для отображения
});
```

**Локализация для таблицы талантов:**
```csv
category;name;rus;eng
feature_attack;normal_hit_1;Урон 1-го удара;1-Hit DMG
feature_attack;charged_hit;Заряженная атака;Charged Attack DMG
feature_attack;stamina_cost;Расход выносливости;Stamina Cost
feature_skill;skill_dmg;Урон навыка;Skill DMG
feature_skill;cd;Время отката;CD
feature_burst;burst_dmg;Урон взрыва;Burst DMG
feature_burst;energy_cost;Энергия;Energy Cost
```

**ВАЖНО: Полные описания талантов**

Описания талантов (`talent_descr`) должны содержать ПОЛНЫЙ текст из игры, а не краткую версию. Это важно для:
- Корректного отображения в окне "Подробности талантов"
- Понимания механик персонажа пользователем
- Соответствия игровым данным

Пример ПРАВИЛЬНОГО описания (Ягода - навык):
```csv
talent_descr;yagoda_cunning_plan;name{Ягода} совершает рывок по направлению к противнику и при соприкосновении с ним входит в состояние Преследующей тени. В этом состоянии name{Ягода} постоянно перемещается на высокой скорости. Если поблизости есть враги с эффектами pyro{Пиро}, hydro{Гидро}, electro{Электро} или cryo{Крио}, name{Ягода} украдкой настраивает свою Секретную буль-буль-фляжку на этот элемент...;...
```

Пример НЕПРАВИЛЬНОГО описания (слишком краткое):
```csv
talent_descr;yagoda_cunning_plan;Элементальный навык Ягоды;Yagoda Elemental Skill
```

### 2. Список условий (ConditionList)

Условия отображаются в табе персонажа и баффов. Каждое условие показывает:
- Контрол (чекбокс/слайдер/dropdown)
- Заголовок
- Метку (C1-C6, A1/A4)
- Описание с подставленными значениями

```javascript
// Статичное описание (только текст, без контрола)
new ConditionStatic({
    title: 'talent_name.passive_name',
    description: 'talent_descr.passive_description',
    info: {ascension: 4},  // Показывает "A4" справа
    stats: {
        text_percent: 20,      // Подставляется в %{text_percent}
        text_value: 100,       // Подставляется в %{text_value}
        text_percent_max: 80,  // Подставляется в %{text_percent_max}
    },
})

// Чекбокс с описанием
new ConditionBoolean({
    name: 'buff_name',
    serializeId: 1,
    title: 'talent_name.buff',
    description: 'talent_descr.buff',
    info: {constellation: 2},  // Показывает "C2" справа
    stats: {
        dmg_all: 15,  // Реальный бонус + переменная для описания
    },
})

// Стаки с dropdown
new ConditionStacks({
    name: 'stacks',
    serializeId: 2,
    title: 'talent_name.stacks',
    description: 'talent_descr.stacks',
    maxStacks: 5,
    stats: [
        new StatTable('atk_percent', [10]),  // 10% за стак
    ],
})
```

**Локализация описаний с переменными:**
```csv
talent_descr;passive_name;Урон увеличивается на %{text_percent}, максимум %{text_percent_max}.;DMG increases by %{text_percent}, max %{text_percent_max}.
talent_descr;buff;При активации урон всех атак увеличивается на %{text_percent}.;When active, all DMG increases by %{text_percent}.
```

**Форматирование в описаниях:**
```
%{stat_name}           - Значение стата
%{stat_name|default}   - Значение или default
%{stat_name|0|2}       - Значение с 2 знаками после запятой

name{Имя}              - Выделение имени персонажа
pyro{Пиро}             - Выделение элемента (цветом)
skill{skill:Навык}     - Кликабельная ссылка на навык
tab{tab-id:Текст}      - Кликабельная ссылка на вкладку
```

### 3. Таблица Features (урон/хил)

Features отображаются в табе "Урон". Каждая feature показывает:
- Название (из локализации `feature_[category].[name]`)
- Иконку элемента
- Обычный урон / Крит урон / Средний урон

```javascript
// Feature для отображения урона
new FeatureDamageSkill({
    name: 'skill_dmg',           // Ключ локализации: feature_skill.skill_dmg
    element: 'hydro',            // Иконка элемента
    isChild: true,               // Показывать с отступом (• Название)
    hits: 3,                     // Показывать "(3 хита)" после названия
    multipliers: [...],
})
```

**Локализация features:**
```csv
category;name;rus;eng
feature_attack;normal_hit_1;Урон 1-го удара;1-Hit DMG
feature_skill;skill_dmg;Урон навыка;Skill DMG
feature_burst;burst_dmg;Урон взрыва;Burst DMG
feature_other;heal_amount;Лечение;Healing
```

### 4. Созвездия

Созвездия отображаются как обычные Condition с меткой C1-C6:

```javascript
new DbObjectConstellation([
    // C1
    {
        conditions: [
            new ConditionStatic({
                title: 'talent_name.char_c1',
                description: 'talent_descr.char_c1',
                stats: {
                    text_value: 100,  // Для описания
                },
            }),
        ],
    },
    // C2 - интерактивный бафф
    {
        conditions: [
            new ConditionBoolean({
                name: 'char_c2_buff',
                serializeId: 5,
                title: 'talent_name.char_c2',
                description: 'talent_descr.char_c2',
                stats: {
                    crit_rate: 20,
                },
            }),
        ],
    },
    // C3 - бонус к навыку (автоматический)
    {
        conditions: [
            new Condition({
                settings: {
                    char_skill_elemental_bonus: 3,
                },
            }),
        ],
    },
    // ...
])
```

**Локализация созвездий:**
```csv
category;name;rus;eng
talent_name;char_c1;Название C1;C1 Name
talent_descr;char_c1;Описание C1 с %{text_value} значением.;C1 description with %{text_value} value.
talent_name;char_c2;Название C2;C2 Name
talent_descr;char_c2;При активации крит. шанс увеличивается на %{text_percent}.;When active, CRIT Rate increases by %{text_percent}.
```

### 5. Детальный просмотр урона (FeatureViewTree)

При клике на "Детали" показывается дерево расчёта урона:
- Базовый урон (множители × стат)
- Бонус урона
- Множитель сопротивления
- Множитель защиты
- Реакция (если выбрана)
- Крит. шанс и крит. урон

Данные берутся автоматически из Feature и её multipliers.

---

## Чеклист для создания нового персонажа

1. [ ] Создать файл `src/js/db/Char/[Name].js`
2. [ ] Добавить данные в `CharTables.js` (или убедиться что генератор их создал)
3. [ ] Добавить данные в `CharTalentTables.js`
4. [ ] Создать файл локализации `data/strings/[version]/[name].csv`
5. [ ] Добавить импорт и регистрацию в `src/js/db/Char.js`
6. [ ] Добавить CSS класс иконки (если нужен кастомный)
7. [ ] Протестировать все features и conditions

### Чеклист локализации

1. [ ] **Имя персонажа** (`char_name`)
2. [ ] **Названия features** (`feature_attack`, `feature_skill`, `feature_burst`, `feature_other`)
3. [ ] **Названия талантов** (`talent_name`) - краткие названия для вкладок
4. [ ] **ПОЛНЫЕ описания талантов** (`talent_descr`) - весь текст из игры с форматированием:
   - Обычная атака: описание всех типов атак
   - Навык: полное описание механики
   - Взрыв: полное описание механики
5. [ ] **Пассивки** с переменными (`%{text_percent}`, `%{text_value}` и т.д.)
6. [ ] **Созвездия** с переменными - указать все числовые значения баффов
7. [ ] **Форматирование**: `name{}`, `pyro{}`, `skill{skill:}`, `skill{burst:}`

---

## Связь данных: что откуда берётся

### Таблица талантов в UI (окно "Подробности талантов")

```
┌─────────────────────────────────────────────────────────────────┐
│  [Обычная атака] [Навык] [Взрыв]  ← Talents.attack/skill/burst  │
├─────────────────────────────────────────────────────────────────┤
│  Описание таланта                 ← talent_descr.char_attack    │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           │ Ур.1  │ Ур.2  │ ... │ Ур.15 │               │   │
│  │───────────┼───────┼───────┼─────┼───────┤               │   │
│  │ Урон 1-го │ 36.7% │ 39.7% │ ... │ 106%  │ ← items[0]    │   │
│  │ Урон 2-го │ 36.7% │ 39.7% │ ... │ 106%  │ ← items[1]    │   │
│  │ КД        │ 10 с  │ 10 с  │ ... │ 10 с  │ ← items[n]    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Название строки: feature_attack.normal_hit_1                   │
│  Значения: charTalentTables.Name.s1.p1                          │
└─────────────────────────────────────────────────────────────────┘
```

### Список баффов/пассивок в UI

```
┌─────────────────────────────────────────────────────────────────┐
│  [✓] Название баффа                                    [A4]     │
│      Описание с подставленными значениями: урон +20%            │
│                                                                 │
│  Контрол ← ConditionBoolean/Stacks/Number                       │
│  Название ← talent_name.buff_name                               │
│  Описание ← talent_descr.buff_name + stats для %{...}           │
│  Метка ← info: {ascension: 4} или {constellation: 2}            │
└─────────────────────────────────────────────────────────────────┘
```

### Таблица урона в UI

```
┌─────────────────────────────────────────────────────────────────┐
│  Атака                                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Урон 1-го удара    [💧]  │ 1234  │ 2468  │ 1851  │       │   │
│  │ Урон 2-го удара    [💧]  │ 1500  │ 3000  │ 2250  │       │   │
│  │ • Доп. урон        [💧]  │  500  │ 1000  │  750  │       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Название ← feature_attack.normal_hit_1                         │
│  Иконка ← element из Feature                                    │
│  Значения ← Feature.getResult() → normal/crit/average           │
│  • отступ ← isChild: true                                       │
└─────────────────────────────────────────────────────────────────┘
```

### Полная цепочка данных для урона

```
charTalentTables.Name.s1.p1          // Сырые множители [36.7, 39.7, ...]
        ↓
new StatTable('normal_hit_1', ...)   // Обёртка с именем
        ↓
Talents.get('attack.normal_hit_1')   // Доступ через DbObjectTalents
        ↓
new FeatureMultiplier({              // Множитель в Feature
    leveling: 'char_skill_attack',
    values: Talents.get('attack.normal_hit_1'),
})
        ↓
new FeatureDamageNormal({            // Feature урона
    name: 'normal_hit_1',
    multipliers: [...]
})
        ↓
Feature.getResult(buildData)         // Расчёт урона
        ↓
{normal: 1234, crit: 2468, average: 1851}  // Результат в UI
```

### Полная цепочка данных для баффа

```
TalentValues.A4Bonus = 20            // Константа значения
        ↓
new ConditionBoolean({               // Условие
    name: 'char_a4_buff',
    stats: {
        dmg_all: TalentValues.A4Bonus,  // Реальный бонус
        text_percent: TalentValues.A4Bonus,  // Для описания
    },
})
        ↓
// Локализация:
// talent_descr.char_a4_buff = "Урон увеличивается на %{text_percent}."
        ↓
// UI показывает: "Урон увеличивается на 20%."
// При активации: stats.dmg_all += 20
```

---

## Примеры типичных паттернов

### Персонаж с инфузией

```javascript
conditions: [
    new Condition({
        settings: {
            allowed_infusion_pyro: 1,  // Разрешить инфузию пиро
        },
    }),
    new ConditionBoolean({
        name: 'char_infusion',
        title: 'talent_name.infusion',
        settings: {
            attack_infusion_pyro: 1,
        },
    }),
]
```

### Персонаж со скейлингом от HP

```javascript
new FeatureDamageSkill({
    name: 'skill_dmg',
    element: 'hydro',
    multipliers: [
        new FeatureMultiplier({
            scaling: 'hp*',  // Важно!
            leveling: 'char_skill_elemental',
            values: Talents.get('skill.skill_dmg'),
        }),
    ],
})
```

### Персонаж с бонусом от EM

```javascript
postEffects: [
    new PostEffectStatsMastery({
        percent: new StatTable('dmg_burst', [0.1]),  // 0.1% за 1 EM
        exceed: 0,  // Считать весь EM
        statCap: new StatTable('', [80]),  // Максимум 80%
        conditions: [
            new ConditionAscensionChar({ascension: 4}),
        ],
    }),
]
```

### Персонаж с уникальным множителем (Furina-style)

```javascript
// Множитель, который применяется только к определённым атакам
multipliers: [
    new FeatureMultiplier({
        scaling: 'hp*',
        source: 'constellation6',
        values: new ValueTable([18]),
        condition: new ConditionBoolean({name: 'char_c6_buff'}),
        target: new FeatureMultiplierTarget({
            tags: ['char_attack'],  // Только атаки с этим тегом
        }),
    }),
]

// Features с тегами
new FeatureDamageNormal({
    name: 'normal_hit_1',
    tags: ['char_attack'],  // Тег для таргетинга
    multipliers: [...],
})
```

### Персонаж с баффом на отряд (Bennett-style)

```javascript
partyData: {
    loadStats: {
        settings: ['char_skill_burst'],  // Загружать уровень взрыва
    },
    conditions: [
        new ConditionBoolean({
            name: 'party.char_buff',
            serializeId: 1,
            rotation: 'party',
            title: 'talent_name.party_buff',
            description: 'talent_descr.party_buff',
            stats: {
                atk_flat: 0,  // Будет рассчитан через postEffect
            },
        }),
    ],
    postEffects: [
        new PostEffectStats({
            from: 'party.char_buff',  // Активируется при включении баффа
            levelSetting: 'char_skill_burst',
            percent: Talents.getAlias('burst.atk_bonus', 'atk_flat'),
            conditions: [
                new ConditionBoolean({name: 'party.char_buff'}),
            ],
        }),
    ],
}
```

### Персонаж с динамическим описанием стаков

```javascript
new ConditionStacks({
    name: 'char_stacks',
    serializeId: 1,
    title: 'talent_name.stacks',
    description: 'talent_descr.stacks',
    maxStacks: 4,
    // Кастомная функция для названий в dropdown
    titleFunc: (stackCount, stats) => {
        if (stackCount == 0) return '0';
        return {
            str: 'talent_name.stack_level_' + stackCount,
            values: stats,
        };
    },
    stats: [
        new StatTable('atk_percent', [10]),
    ],
})
```

### Персонаж с условным максимумом стаков

```javascript
new ConditionStacks({
    name: 'char_stacks',
    serializeId: 1,
    title: 'talent_name.stacks',
    // Максимум зависит от созвездия
    maxStacks: (settings) => {
        return settings.char_constellation >= 2 ? 5 : 3;
    },
    stats: [...],
})
```

### Персонаж с несколькими скейлингами в одном ударе

```javascript
new FeatureDamageSkill({
    name: 'skill_dmg',
    element: 'dendro',
    multipliers: [
        // ATK скейлинг
        new FeatureMultiplier({
            scaling: 'atk*',
            leveling: 'char_skill_elemental',
            values: Talents.get('skill.skill_dmg_atk'),
        }),
        // + EM скейлинг
        new FeatureMultiplier({
            scaling: 'mastery*',
            leveling: 'char_skill_elemental',
            values: Talents.get('skill.skill_dmg_em'),
        }),
    ],
})
```

### Персонаж с отображением PostEffect значения

```javascript
// Показать рассчитанный бонус в таблице урона
new FeaturePostEffectValue({
    category: 'burst',
    name: 'calculated_bonus',
    postEffect: myPostEffect,  // Ссылка на PostEffect
    format: 'percent',  // Формат отображения
    condition: new ConditionAscensionChar({ascension: 4}),
})
```
